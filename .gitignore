# CMake-generated build system (regenerate with `cmake .`; CMakeLists.txt is source of truth)
build/
CMakeFiles/
/Makefile
cmake_install.cmake
CMakeCache.txt
compile_commands.json

# Compiled objects and binaries
*.o
*.out
*.bin
*.exe
kvstore

# Editor / tooling scratch
.claude/

# Runtime secrets — never commit a filled-in .env (template is deploy/aws/.env.example)
.env
**/.env
!*.env.example
